# Block color pickers

Categories, household members, and the browser-local accent use `react-color`'s BlockPicker and persist six-digit hex values. Member-color validation and its pending migration accept any valid hex value. The local accent derives primary, focus, sidebar, and chart tokens from the selected value without changing positive, negative, expense, or destructive tokens. Existing named accent cookies migrate to their equivalent hex values.
